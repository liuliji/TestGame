defmodule Websocket.ServerUser do
    alias Entice.Entity
    require Logger

    @doc """
    User struct.
    :uid, :roomId, :userName, :socketId
    """
    defmodule User do
        defstruct(
            uid: "",
            roomId: "",
            userName: "",
            socketId: ""
            )
    end

    def 

end